<ul>
  {{#each persons}}
    {{#with this}}
      {{#if persisted}}
      <li data-identifier="{{identifier}}">{{name}}</li>
      {{else}}
      <li><a href="#" data-behavior="create-person" data-identifier="{{identifier}}">{{name}}</a></li>
      {{/if}}
    {{/with}}
  {{/each}}
</ul>
