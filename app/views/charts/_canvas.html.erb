<%
  chart ||= nil
%>

<%= link_to image_tag("btn-editor.png"), "#", class: "show-editor" %>
<% if @node %>
  <%= link_to @node.title, "#", class: "show-breadcrumb" %>
<% end %>

<div class="canvas" data-init="chart" data-action="<%= params[:action] %>">
  <div id="canvas" data-no-turbolink></div>
</div>
